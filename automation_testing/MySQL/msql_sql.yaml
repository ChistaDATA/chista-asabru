read:
  r1: "SELECT * FROM classicmodels.employees"
  r2: "SELECT * FROM classicmodels.employees WHERE firstName = 'George'"
  r3: "SELECT jobTitle, count(jobTitle) FROM classicmodels.employees GROUP BY jobTitle"
  r4: "SELECT COUNT(DISTINCT employeeNumber) FROM classicmodels.employees"
  r5: "SELECT e.employeeNumber , e.firstName, o.city FROM classicmodels.offices AS o INNER JOIN classicmodels.employees AS e ON  e.officeCode = o.officeCode"