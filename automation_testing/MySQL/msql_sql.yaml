read:
  r1: "SELECT * FROM employees.employees"
  r2: "SELECT * FROM employees.employees WHERE first_name = 'Chirstian'"
  r3: "SELECT gender, count(gender) FROM employees.employees GROUP BY gender"
  r4: "SELECT COUNT(DISTINCT emp_no) FROM employees.employees"
  r5: "SELECT s.emp_no , e.first_name, s.salary FROM employees.salaries AS s INNER JOIN employees.employees AS e ON  e.emp_no = s.emp_no"